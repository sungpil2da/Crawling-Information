{
  "name": "ig-auto-dm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "test": "node test/match.test.mjs && node test/webhook.test.mjs",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.0"
  },
  "engines": {
    "node": ">=20"
  }
}
